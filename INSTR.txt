คือการค้นหาตำแหน่งจากรสตริง paramiterที่1 ด้วย paramiterที่2 มันจะคืนค่าตำแหน่งตัวแรกที่หาเจอมาให้ ถ้าหาไม่เจอจะคืนค่ามาเป็น 0

SELECT INSTR('foobarbar', 'bar');
-> 4

SELECT INSTR('xbar', 'foobar');
-> 0


